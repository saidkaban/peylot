import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

interface Props {
  
}

const ItemDetailScreen = (props: Props) => {
  return (
    <View>
      <Text></Text>
    </View>
  )
}

export default ItemDetailScreen

const styles = StyleSheet.create({})
