import React from "react";
import { View, Text } from "react-native";

interface Props {}

const ProfileScreen = (props: Props) => {
  return (
    <View>
      <Text>My ProfileScreen</Text>
    </View>
  );
};

export default ProfileScreen;
